services:
  - type: web
    name: mariadb-service
    env: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: PORT
        value: 8080
    # Especificar el puerto HTTP que Render debe buscar
    port: 8080
